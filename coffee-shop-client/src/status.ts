export enum LOGIN_WITH_EMAIL_STATUS {
  SUCCESS,
  WRONG_PASSWORD,
  INVALID_EMAIL,
  ERROR,
}

export enum REGISTER_WITH_EMAIL_STATUS {
  SUCCESS,
  INVALID_EMAIL,
  EMAIL_ALREADY_EXISTS,
  ERROR,
}

export enum LOGIN_WITH_GOOGLE_STATUS {
  SUCCESS,
  ERROR,
}

export enum AUTH_LISTENER_STATUS {
  LOGGED_IN,
  LOGGED_OUT,
}

export enum SIGNOUT_STATUS {
  SUCCESS,
  ERROR,
}

export enum PRODUCTS_STATUS {
  PERMISSION_ERROR,
  FETCHED,
  ERROR,
}
